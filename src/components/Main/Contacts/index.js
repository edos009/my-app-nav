import React from "react";
import styles from "../../../App.module.scss";

const Contacts = () => {
  return (
    <div className={styles.container}>
      <div className={styles.main_box}>Contacts</div>
    </div>
  );
};

export default Contacts;
